--1. normalize the data in the orders table

-- create view customers as
--     select
--         customer_id,
--         customer_name,
--         customer_segment,
--         city,
--         state,
--         region,
--         postal_code
-- from orders

select * from customers;